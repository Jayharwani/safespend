/**
 * Ambient layer — a flat ink background with a single, very faint jade sheen
 * at the top. No blobs, no animation, no glow. Quiet depth only.
 */
export default function AuroraBackground() {
  return <div className="aurora" aria-hidden />;
}
